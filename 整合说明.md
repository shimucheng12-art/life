# 整合版说明 —— 美好生活记录 + Oyama's HRT Tracker

本工程是 **「美好生活记录」单文件应用** 与 **Oyama's HRT Tracker** 的整合版，
以「美好生活记录」为 APP 首页，通过 Tauri 打包为桌面应用（Windows MSI / macOS DMG）。

---

## 一、整合内容与改动点

共改动 **3 个文件**（其余全部保持原样）：

| 文件 | 改动 |
|---|---|
| `public/life.html` | **新增**（来自 `code_20260924(11).html`）。「HRT记录」标签从跳转外部网址改为 `switchTab('hrt')`；`page-hrt` 引导页按钮改为调用 `openHrt()` 在应用内全屏内嵌加载 HRT Tracker；文件末尾新增内嵌层（顶栏 + iframe + 加载提示）的样式、结构与逻辑 |
| `index.html` | 顶部新增一段跳转脚本：直接访问时 `location.replace('./life.html')` 进入首页；带 `?embed=1` 参数（iframe 内嵌）时正常加载 HRT Tracker 本体 |
| `src-tauri/tauri.conf.json` | 主窗口标题由 "Oyama's HRT Tracker" 改为 "美好生活记录"（其余配置未动） |

### 运行逻辑

```
APP 启动
  └─ dist/index.html（HRT Tracker 入口）
       └─ 无 embed 参数 → 立即跳转 life.html（美好生活记录 = 首页）
            └─ 底部标签「HRT记录」→ page-hrt 引导页 →「打开 HRT 记录工具」
                 └─ 全屏 iframe 加载 index.html?embed=1（HRT Tracker 本体）
                      └─ 顶栏「‹ 返回」可随时回到美好生活记录
```

- 两个应用数据都存在同一 localStorage 中，键名互不冲突（生活记录用
  `appUsers`/`curUser`/`xxx_用户名`；HRT Tracker 用 `app-`/`auth_`/`hrt-` 等前缀），互不影响。
- 打包后 HRT Tracker 完全离线可用；其「登录 / 云同步」功能依赖 Cloudflare 后端，
  离线 APP 内不可用（本地记录、浓度模拟、导出等核心功能不受影响）。

---

## 二、本地打包桌面 APP

前置要求：Node.js 18+、Rust 工具链（`rustup` 安装）。

```bash
npm install          # 或 pnpm install / bun install
npm run tauri:build  # 首次构建需编译 Rust，耗时较长
```

产物位置：

- **Windows**：`src-tauri/target/release/bundle/msi/*.msi`
- **macOS**：`src-tauri/target/release/bundle/macos/*.dmg`（及 `.app`）

仅想构建网页部分做验证：`npm run build`，产物在 `dist/`（其中 `life.html`
为首页入口，`index.html` 为 HRT Tracker 本体）。

---

## 三、用 GitHub Actions 云端打包（无需本地装 Rust）

工程自带 `.github/workflows/tauri-build.yml`：

1. 把整合后的工程推送到 GitHub 仓库；
2. 打一个 `v*.*.*` 形式的 tag（或在 Actions 页面手动 Run workflow）；
3. 构建完成后在 Actions Artifacts 中下载 `tauri-windows-msi` 与 `tauri-macos-dmg`。

---

## 四、日常开发

```bash
npm run dev        # 网页开发模式，浏览器打开 http://localhost:3000
                   # （会自动跳到 life.html 首页，与打包行为一致）
npm run tauri:dev  # 桌面壳开发模式
```

---

## 五、自定义入口（如需改回）

- 想让 HRT Tracker 直接当首页：删除 `index.html` 顶部 `<head>` 内的跳转脚本即可。
- 「HRT记录」标签想恢复跳外部网址：把 `public/life.html` 中
  `data-tab="hrt"` 一行的 `onclick="switchTab('hrt')"` 改回
  `onclick="window.open('https://mtf-hrt-03.netlify.app/', '_blank')"`。

---

## 六、致谢

HRT Tracker 的药代动力学算法与参数来自
[HRT-Recorder-PKcomponent-Test](https://github.com/LaoZhong-Mihari/HRT-Recorder-PKcomponent-Test)（@LaoZhong-Mihari），
原工程遵循 MIT License，公开部署时请保留算法鸣谢。
